import { IconInlineComponent } from './inline-svg.component';

describe('Component: IconInline', () => {
  it('should create an instance', () => {
    let component = new IconInlineComponent();
    expect(component).toBeTruthy();
  });
});
