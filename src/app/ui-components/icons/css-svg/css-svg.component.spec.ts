import { IconCssComponent } from './css-svg.component';

describe('Component: IconCss', () => {
  it('should create an instance', () => {
    let component = new IconCssComponent();
    expect(component).toBeTruthy();
  });
});
