import { IconResizeComponent } from './resize-svg.component';

describe('Component: IconResize', () => {
  it('should create an instance', () => {
    let component = new IconResizeComponent();
    expect(component).toBeTruthy();
  });
});
