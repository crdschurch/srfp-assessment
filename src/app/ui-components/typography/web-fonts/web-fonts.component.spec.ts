import { TestBed } from '@angular/core/testing';
import { WebFontsComponent } from './web-fonts.component';

describe('Component: WebFonts', () => {
  it('should create an instance', () => {
    let component = new WebFontsComponent();
    expect(component).toBeTruthy();
  });
});
